module tb;
    logic clk = 0;
	 logic switch = 0;
	 logic PCMSB;
	 logic [35:0] dummy;
    always #1 clk = ~clk;
		
    top dut (clk, switch, PCMSB, dummy);
	 
	 initial begin
		switch = 1;
		#3;
		switch = 0;
		#200;
	 end
endmodule